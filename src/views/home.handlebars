<h1>Productos</h1>
<div>
    {{!-- Recibe el objeto products del view --}}
    {{#each products}}
    <div>
        <p>{{this.title}}</p>
        <p>{{this.description}}</p>
        <p>{{this.price}}</p>
        <p>{{this.id}}</p>
    </div>
    {{/each}}
</div>